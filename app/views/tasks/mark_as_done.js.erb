var elem_id = '<%= @task.id %>'
var new_url = '<%= mark_as_undone_task_path %>';
var new_image_path = '<%= asset_path "checked_icon.png" %>';

$('a.task-status#' + elem_id).attr('href', new_url);
$('a.task-status#' + elem_id + ' > img').attr('src', new_image_path);